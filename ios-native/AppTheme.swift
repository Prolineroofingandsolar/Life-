import SwiftUI

// MARK: - App Theme

enum AppTheme {
    static let green            = Color(hex: "#30d158")
    static let accent           = Color(hex: "#30d158")
    static let card             = Color(.secondarySystemGroupedBackground)
    static let surface          = Color(.systemGroupedBackground)
    static let border           = Color.white.opacity(0.08)
    static let label            = Color.primary
    static let sublabel         = Color.secondary
    static let streakActive     = Color(hex: "#30d158")
    static let streakInactive   = Color(.systemFill)
    static let progressTrack    = Color.white.opacity(0.12)
    static let progressFill     = Color(hex: "#30d158")
    static let completedFill    = Color(hex: "#30d158")
    static let incompleteFill   = Color(.systemFill)
}
